# 《造梦西游》素材整理规范

状态：2026-07-21 起生效  
适用范围：《造梦西游 1》《造梦西游 2》《造梦西游 3》及后续版本的提取、分类、去重、命名、筛选和运行时选材。

## 目标

素材整理需要同时满足三件事：

1. 能从原 SWF 和代码追溯素材来源；
2. 分类目录简洁、可理解，不堆放运行时可生成的冗余项；
3. Godot 项目只使用实现功能所需的标准素材。

## 三层目录职责

### 1. 原始依据与临时拆包层

- `sources/raw`
- `sources/decoded`

`sources/raw` 与 `sources/decoded` 长期保存原始及解码 SWF，作为可重新提取的最终依据。

- `assets/extracted/full/<game>`

`full` 只作为临时完整拆包区：整理期间保持只读，供代码、`symbolClass`、FrameLabel 和引用链审计；当该版本的分类、删除与来源映射已经写入清单并通过数量/哈希核验后，应将可用素材移动或硬链接落位到 `classified`，随后删除对应 `full/<game>`。不要长期保留一棵与标准库并行的完整元件树。

### 2. 标准分类层

- `assets/extracted/classified/<game>`

这是人工整理后的唯一规范素材库。统一语义目录和名称，删除重复格式、空白图、运行时冗余帧及无保留价值的分类副本。无法可靠分类但仍有内容的素材统一进入 `杂项/<来源包或原语义>/`，不能因为暂时未知而丢弃，也不再散落于各正式分类下的多个“待识别”目录。

### 3. 开发选材层

- `assets/selected/<game>`
- `assets/ui`、`resources` 等运行时目录

这里只放实际开发确认采用的素材和配置。选材层可以加工、裁切或规范化，但必须保留来源清单，不能代替溯源层。

## 移动式更新

- 每项可保留素材最终只能有一个规范分类路径；已知素材进入正式语义目录，未知素材进入顶层 `杂项`。
- `full` 中的原路径、包名、符号、时间轴和规范目标路径必须先写入 `sources/manifests`。
- 分类完成后以文件数、字节数、哈希或硬链接实体核对迁移结果；确认无遗漏后删除该游戏的 `full`。
- 空白帧、已验证的数值中间帧、字节/视觉等价副本和非素材结构导出物不迁入 `classified`，其删除理由仍须保留在清单中。
- 原始与解码 SWF 不随 `full` 清理而删除；需要复查时从 SWF 重新提取到临时 `full`。

## 判断顺序

整理一组素材前，依次确认：

1. 它来自哪个 SWF、符号、脚本或时间轴；
2. 它在原游戏中承担什么功能；
3. 多张图之间属于真实动画、数值变化、语义枚举、组合零件，还是重复格式；
4. Godot 运行时需要逐帧资源，还是可以由控件、文本、Shader 或组合节点生成；
5. 最终保留项是否足以复现原效果。

源码、`symbolClass`、FrameLabel 和实际调用优先于文件名猜测。无法确认的素材进入待确认目录，不强行命名或删除。

## 通用保留与精简规则

### 真实动画

动作、特效、闪光、变色、波纹、粒子或形变等随时间改变画面内容的序列，应保留有效动画帧。删除时只处理完全空白、重复停顿或已验证无引用的帧。

#### 嵌套时间轴动画

Flash 根 `MovieClip` 停在 `FrameLabel`（如 `hit1`）时，已放置的子 `MovieClip` 仍可独立播放。因此根时间轴导出的少量 PNG 只能作为定位/组合参考，不能据此缩短子时间轴。整理角色或怪物动作时，必须沿 `FrameLabel → PlaceObject.characterId → DefineSprite` 追踪；凡由该动作放置且 `DefineSprite.frameCount > 1` 的子时间轴，均作为完整动作时间轴保留。若同一子时间轴被多个动作复用，应放入“共享动作时间轴”并在清单中记录全部动作引用，而不能任意归入某一个动作或删除。

### 数值驱动时间轴

如果 Flash 只是依据生命、魔法、经验、能量、加载进度等数值调用 `gotoAndStop(...)`，而各帧只改变同一图形的可见长度：

- 每种样式只保留一张 100% 完整填充图；
- 底槽、边框、头像和文字底板等必要静态图层分别保留；
- 0%～99% 的中间帧不进入标准分类库；
- Godot 使用 `TextureProgressBar`、裁切区域或 Shader 生成比例；
- 需要复刻原版整数跳变时设置 `step = 1`，或先把比例四舍五入为整数。

完整填充帧不固定是第 1 帧，必须结合代码公式和画面确认。例如：

- `gotoAndStop(round(100 * (1 - current / maximum)) + 1)`：通常第 1 帧是满值；
- `gotoAndStop(round(100 * current / maximum))`：通常末端帧是满值。

### 语义枚举

Boss 名称、角色头像、装备等级、按钮状态等虽然可能位于同一个 MovieClip 中，但每帧含义不同。此类素材按语义命名并保留所有实际使用的变体，不能当作百分比冗余帧删除。

### 组合零件

角色换装、武器、防具和分层特效需要根据原代码的组合方式整理。单张零件不能被误认为完整皮肤；应记录角色、部位、showid、层级、锚点和来源符号。

### 文本、数字与简单图形

普通文本、伤害数字、纯色底板和简单边框优先由 Godot 字体、Theme、StyleBox 或 Shader 生成。只有原作字体造型、纹理或特殊效果无法等价生成时，才保留位图。

### 重复格式与重复文件

- 字节完全相同：保留一个标准路径。
- PNG、SVG、JPG 视觉等价：优先保留透明度和像素还原最可靠的格式，删除分类副本前记录比对证据。
- 同一素材被多个包重复导出：标准分类层只保留一个规范副本，清单记录全部来源。
- 不修改原始及解码 SWF 来实现分类去重；临时 `full` 在清单与核验完成后整体清理。

## 命名规则

- 顶层使用稳定语义：`人物 / 怪物 / UI / 场景与地图 / 公共元件`。
- 动画使用动作名和补零序号：`idle_001.png`、`attack1_001.png`、`hurt_001.png`。
- 数值条使用功能名：`hp_slider.png`、`mp_slider.png`、`energy_slider.png`、`exp_slider.png`；底槽追加 `_bg`。
- 装备和换装保留 `showid`：`showid_04/part_177.png`。
- 未确认内容使用待确认目录和中性编号，不把猜测写入正式名称。
- 文件名表示素材语义，不沿用没有信息量的 `ui_0001`，除非暂时无法可靠识别。

## 删除与记录要求

标准分类层的删除必须满足至少一项：

- 已确认是数值驱动中间帧；
- 已确认是空白或无内容帧；
- 已确认与保留项字节相同或视觉等价；
- 已确认是提取器产生、运行时不会使用的冗余副本；
- 有代码、符号或引用链证据证明不需要保留。

每次整理在 `sources/manifests` 记录：

- 游戏版本与来源 SWF；
- 原符号、脚本或时间轴；
- 原始数量和分类；
- 保留项及其标准路径；
- 删除范围与判断理由；
- Godot 中的替代实现；
- 尚未确认的问题。

## 标准工作流程

1. 只读检查原 SWF、完整拆包、代码和现有清单。
2. 建立候选清单，区分动画、数值、枚举、零件和重复项。
3. 抽查关键帧或组合效果，确认判断。
4. 先确定标准目录、名称和唯一保留项。
5. 更新规则或版本清单。
6. 将已知素材落位到正式分类，将未知但有内容的素材移动到顶层 `杂项`。
7. 清理标准分类层中的冗余项，检查文件数量、哈希、JSON 和引用路径。
8. 确认规范素材均有唯一目标后删除对应 `assets/extracted/full/<game>`。
9. 在 Godot 中验证关键状态或动画。

## 造梦西游 1：百分比 HUD 示例

原版 `RoleInfo.step()` 对 `hpline`、`mpline`、`expline` 调用 `gotoAndStop(...)`，按比例在约 101 帧之间跳转；`wsmc` 也按数值选择时间轴帧。这些帧只有填充长度变化，因此标准分类库只保留：

- `hp_slider.png` 与 `hp_slider_bg.png`
- `mp_slider.png`
- `energy_slider.png`
- `exp_slider.png` 与 `exp_slider_bg.png`

Boss 名称的 11 张图含义不同，继续保留。造 1 在提交 `babd91b` 中已从分类库删除 321 项冗余 HUD 素材；详细记录见：

`sources/manifests/zmxiyou1_hud_percentage_cleanup.json`

## 造 2、造 3 与后续版本

后续版本不照搬造 1 的具体文件名或帧号，但必须照搬本规范的判断流程。每个版本都要重新核对代码公式、时间轴方向、实际画面和运行时需求，再形成自己的版本清单。
